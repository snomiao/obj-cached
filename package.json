{
  "name": "obj-cached",
  "module": "index.ts",
  "main": "dist/index.js",
  "files": ["dist/*"],

  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "tsc && bun build ./ --outdir=dist",
    "prepublish": "npm version patch"
  },

  "version": "0.0.2",
  "dependencies": {
    "typescript": "latest"
  }
}