{
  "name": "obj-cached",
  "module": "dist/index.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "tsc && bun build ./ --outdir=dist",
    "prepublishOnly": "bun run build && npm version patch"
  },
  "version": "0.0.10",
  "dependencies": {
    "typescript": "latest"
  }
}
