{
  "name": "obj-cached",
  "module": "dist/index.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*"
  ],
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "rm -rf dist && tsc && bun build ./ --outdir=./dist",
    "prepack": "bun run build && npm version patch"
  },
  "version": "0.0.13",
  "dependencies": {
    "typescript": "latest"
  }
}
