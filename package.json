{
  "name": "obj-cached",
  "module": "index.ts",
  "main": "dist/index.js",
  "files": [
    "dist/*"
  ],
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "tsc && bun build ./ --outdir=dist",
    "prepublishOnly": "npm version patch"
  },
  "version": "0.0.3",
  "dependencies": {
    "typescript": "latest"
  }
}
